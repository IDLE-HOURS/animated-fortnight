export default (function (domNodeList, callback) {
  var mappedReturns = [];

  for (var i = 0; i < domNodeList.length; i++) {
    mappedReturns.push(callback(domNodeList[i], i));
  }

  return mappedReturns;
});